/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/lsb-ray-rodriguez-law-office-hialeah',
  images: { unoptimized: true },
};
module.exports = nextConfig;
